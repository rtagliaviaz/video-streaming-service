import React from 'react';

export const VideoListEmpty: React.FC = () => (
    <p style={{ textAlign: 'center', padding: '1.5rem 0', opacity: 0.6 }}>
        No processed videos yet. Upload one!
    </p>
);